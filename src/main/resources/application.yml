spring:
  # 1. (추가) DB 설정 (application.properties에서 가져옴)
  datasource:
    url: jdbc:mysql://localhost:3306/tikitta?serverTimezone=Asia/Seoul
    #url: jdbc:mysql://15.164.218.55:3306/tikitta?serverTimezone=Asia/Seoul
    username: tikkita
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: 6eda97bf3bccba17cfc112c90731a2c9
            client-secret: quFa6gCqnBZUtwdrUIQab9JBptBQRwEL
            client-name: Kakao
            redirect-uri: "https://back-tikitta.duckdns.org/login/oauth2/code/kakao"
            authorization-grant-type: authorization_code
            client-authentication-method: client_secret_post
            scope:
              - profile_nickname
              - account_email
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id
server:
  forward-headers-strategy: framework
  servlet:
    session:
      cookie:
        same-site: none
        http-only: false
        secure: true
frontend-url: http://localhost:5173

# Gabia SMS API Settings
gabia:
  sms:
    id: "YOUR_SMS_ID" # 여기에 실제 SMS ID를 입력하세요.
    api-key: "YOUR_API_KEY" # 여기에 실제 API Key를 입력하세요.
    callback: "YOUR_CALLBACK_NUMBER" # 여기에 실제 발신번호를 입력하세요.
